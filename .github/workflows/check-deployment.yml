name: Check Deployment
on:
  workflow_dispatch:
    inputs:
      passci:
        description: 'Pass CI'
        required: true
        type: boolean
  pull_request:
  issue_comment:
    types: [created, edited]

jobs:
  check-branches:
    uses: iterative/gatsby-theme-iterative/.github/workflows/check-deployment.yml@check-deployment-workflow
    with:
      passci: ${{ inputs.passci }}
