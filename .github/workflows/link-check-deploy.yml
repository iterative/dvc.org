name: Check new links against deployment
on:
  deployment_status:
jobs:
  run:
    uses: iterative/link-check/.github/workflows/link-check-deployment-status.yml@test-log-deployment-status-event
    if:
      github.event.deployment.ref == ${{ inputs.branch }} &&
      github.event.deployment_status.state == 'success'
